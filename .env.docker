# Docker Environment Variables for Ward 14 Voting Tracker

# Django Settings
SECRET_KEY=your-super-secret-key-change-this-in-production
DEBUG=False

# Database Settings (embedded PostgreSQL)
POSTGRES_DB=voting_tracker_db
POSTGRES_USER=voting_admin
POSTGRES_PASSWORD=voting_secure_pass_change_this

# Note: ALLOWED_HOSTS and CORS are configured to accept all origins
# This is suitable for trusted internal use

# Superuser Credentials (created automatically on first run)
# Default: admin / admin123
# Change password immediately after first login!
